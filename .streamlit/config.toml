[theme]
base = "light"
primaryColor = "#1f275f"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f2f2f5"
textColor = "#14140f"
font = "sans serif"

[server]
# No first-run e-mail prompt, no browser pop-up from the terminal.
headless = true

[browser]
gatherUsageStats = false
